export type Gem = {
  slug: string;
  name: string;
  origin: string;
  cut: string;
  color: string;
  carat: string;
  price: string;
  image: string;
  description: string;
  featured?: boolean;
};